# Integration Tests

This directory contains HTTP and process integration tests for the Gateway service.

## Local HTTP fixtures (default)

Routine tests use dummy credentials and an owned loopback OpenAI simulator. They exercise the
**shared production router** (`gateway::app::build_production_router`) and the **real Reqwest
adapter**, not only `LLMVendor` mocks.

| File                                                     | What it covers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `http_fixture_test.rs`                                   | Two isolated production-router runs: `/health`, `/ready`, authenticated generation, metrics/correlation, loopback bind and drop cleanup, shared-builder call sites                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `health_readiness_test.rs`                               | Production-router liveness vs readiness with real Supabase and the owned simulator: independent database and `/models` outages, success-only cache expiry and immediate recovery, missing-schema 503, SELECT-only/wrong-column UPDATE runtime-role 503 until `last_used_at` UPDATE is granted, revoked-key persistence, and default-route circuit override of cached upstream health                                                                                                                                                                                                                                                                                                                         |
| `admission_limits_http_test.rs`                          | Protected-router raw-body and metadata bounds plus concurrent generation admission: inclusive `max_request_body_bytes` on declared `Content-Length` and chunked/no-length bodies for inference and key-create JSON, advertised oversize `413 PAYLOAD_TOO_LARGE` envelopes with correlation, metadata `max_metadata_bytes` `400 INVALID_REQUEST`, zero generation calls on rejection, configured concurrency N with held simulator work, prompt `429 OVERLOADED` plus `Retry-After` and zero extra provider calls, `/health`/`/ready`/`/metrics` remaining responsive while slots are occupied, retry/fallback permit hold, and release after success, oversized upstream failure, deadline, and cancellation |
| `openai_adapter_http_test.rs`                            | Actual OpenAI adapter Chat Completions wire, provider-reported model, selected-target cost, same-requested-model different prices, missing-price failure, malformed/invalid success payloads including non-assistant roles, bounded and chunked bodies, scripted 500, stalled 429 headers, 429 quota code/type, malformed/oversized 429 bodies, inherited env/proxy isolation                                                                                                                                                                                                                                                                                                                                |
| `openai_results_http_test.rs`                            | Production-router Chat Completions protocol, named-target prices, same-requested-model different prices, fallback cost ownership, malformed success payloads including non-assistant roles without retry, and response-size bounds across Content-Length and chunked transfer                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `observability_integration_test.rs`                      | Correlation, metrics, health/ready, circuit-open via the shared router                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `observability_metrics_test.rs`                          | Production-router `/metrics` deltas for success, retry, fallback, circuit open/recovery, deadline, and local overload against the owned simulator; HTTP auth-failure counts; bounded endpoint/target/outcome labels with no request-ID, key-path, unknown-route, or reported-model series; isolated Prometheus recorder reused across serial scrapes                                                                                                                                                                                                                                                                                                                                                         |
| `observability_correlation_test.rs`                      | Fresh `gateway=debug` JSON captures for success, missing/invalid key, capability 403, datastore 503, malformed body, local overload, and upstream failure: response `X-Request-ID` equals `error.request_id` and request-scoped logs, valid `X-Correlation-ID` is echoed into logs, dummy credential/prompt/metadata/SQL/provider sentinels are absent, and authentication `auth_duration_ms` excludes simulator delay while including a controlled auth-store delay. Unknown, persisted-revoked, and datastore-unavailable captures assert one HTTP-boundary terminal event, no AuthService validation-failed duplicate, retained auth timing/outcome, and zero provider calls                              |
| `process_lifecycle_test.rs`                              | Occupied-port binary startup leaves the fixture listener intact; SIGTERM/SIGINT drain the production router and owned subprocesses, override cached readiness, reject new generation with `503 DRAINING` or report connection refusal, complete in-flight work within grace, and cancel slow provider/retry-backoff work at `SERVER_SHUTDOWN_TIMEOUT` without later simulator attempts. Cold-cache held `/models` readiness still reports draining after drain starts; Application admission stays closed after a stale not-draining observation and after releasing a predating permit. Cleanup uses owned PIDs only                                                                                        |
| `tls_verification_test.rs`                               | Bounded Reqwest client against an owned untrusted local TLS simulator: handshake fails before HTTP success, certificate/hostname verification stay enabled, no accept-invalid shortcut. Image runtime coverage is `scripts/check-container.sh`                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `config_startup_test.rs` / `startup_integration_test.rs` | Binary catalog/logging startup with canonical `OPMUX_CONFIG_FILE`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `auth_store_test.rs`                                     | Real owned-Supabase schema, grant, constraint, and SQLx store tests. Requires owned `DATABASE_URL`; does not skip                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `provisioning_test.rs`                                   | Shared provisioning service: two-tenant identity, digest-only storage, rollback, reconnect. Requires owned `DATABASE_URL`; does not skip                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `opmux_admin_test.rs`                                    | Actual `opmux-admin` subprocesses against real Supabase. Captures secrets privately; does not print them                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `auth_http_test.rs`                                      | Production-router persisted auth, secret-safe debug captures, and same-instance database outage recovery through a loopback TCP proxy                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `key_management_http_test.rs`                            | Production-router management/inference capability matrix, HTTP key creation, one-time secret, and ownership/kind rejection against real Supabase                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `key_lifecycle_test.rs`                                  | Production-router DELETE revocation, last-used persistence, two PID-owned gateway processes for immediate cross-instance revocation, restart durability, manager rotation, and `opmux-admin` recovery                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `routing_http_test.rs`                                   | Configured default/named route selection, unknown-route zero-call denial, opaque metadata, and stateless prompts through the production router and owned simulator                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `execution_budget_test.rs`                               | Protected-request deadline across slow body, delayed real-DB auth, and stalled simulator headers/bodies; 504 envelopes; health/metrics outside the deadline; one-slot runtime pool release after a timed-out row-lock wait so an unrelated key can authenticate while the blocker remains held; real-adapter cancellation of in-flight and backoff work; shared attempt budgets; capped jitter and Retry-After cannot-fit 429 vs expired 504, including configured fallbacks; stalled 429 cannot-fit before a 400ms deadline; fitting Retry-After 1 with stalled body then a held retry returning 504; near-attempt-cutoff 429 headers; malformed/oversized 429 bodies retaining throttling                  |
| `fallback_http_test.rs`                                  | Same-provider A→B→C fallback order through the production router and real adapter; shared attempt budget; `allow_fallback` opt-out; mixed-cap skip without parameter changes; eligibility matrix for transient vs credential/quota/protocol/throttling failures; actual 429 quota code/type with queued success; same-router threshold-1 throttling `[A,A]` never B; throttled probe does not reopen; preserved primary error and deadline override; target-scoped circuits, skipped-open accounting, half-open single-flight probe, and success/failure recovery. Generation-owned stale-completion races live in `gateway/src/features/executor/circuit_tests.rs` (retained barriers, not lossy Notify).   |
| `owned_database_guard_test.rs`                           | Fake-Docker wrapper, mutating-helper URL rejection, and documented private CLI output recipe. Does not need a real database                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `support/`                                               | Environment isolation, loopback simulator, recoverable loopback DB proxy, owned-database URL guard                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

Fixtures bind `127.0.0.1` with an OS-assigned port and abort the owned task on drop. They clear
inherited `OPENAI_*`, `ANTHROPIC_API_KEY`, and proxy variables, set `NO_PROXY=*`, and inject dummy
loopback settings explicitly.

```bash
env -u OPENAI_API_KEY -u ANTHROPIC_API_KEY -u HTTP_PROXY -u HTTPS_PROXY -u ALL_PROXY \
  -u http_proxy -u https_proxy -u all_proxy \
  OPENAI_BASE_URL=http://127.0.0.1:9/v1 NO_PROXY='*' \
  cargo test -p gateway --test http_fixture_test --test openai_adapter_http_test
```

Do not treat these simulator results as live OpenAI verification.

## Persistence tests (owned local Supabase)

`auth_store_test.rs` talks to the owned Postgres on `127.0.0.1:55432`. Apply migrations first, then
inject `DATABASE_URL` without printing it. `scripts/with-owned-database.sh` verifies the owned
container and replaces inherited remote or unrelated-local URLs. Direct test helpers reject
non-owned destinations and extra connection options.

```bash
bash scripts/with-owned-database.sh bash scripts/db-migrate.sh
bash scripts/with-owned-database.sh env -u OPENAI_API_KEY -u ANTHROPIC_API_KEY \
  -u HTTP_PROXY -u HTTPS_PROXY -u ALL_PROXY -u http_proxy -u https_proxy -u all_proxy \
  OPENAI_BASE_URL=http://127.0.0.1:9/v1 NO_PROXY='*' \
  cargo test -p gateway --test auth_store_test
```

If the database is down or the schema is missing, these tests fail with a setup message. They are
not ignored and they do not skip.

`provisioning_test.rs` and `opmux_admin_test.rs` also require the owned `DATABASE_URL`. They
exercise tenant create, existing-client key issue, identity resolution, and failed-issuance
rollback. Successful CLI JSON includes a one-time credential; tests capture it in a fresh private
file or in memory and never print it. Help text covers the `mktemp`/`chmod 600` stdout recipe and
`opmux_operator` versus migration privileges.

`auth_http_test.rs` captures fresh `gateway=debug` diagnostics through the shared production router
for accepted, rejected, and datastore-failure authentication. It asserts those captures are nonempty
and omit presented credentials and stored digest representations without printing them. Transient
outage recovery uses one long-lived `AuthService`/pool behind a recoverable loopback TCP proxy: an
existing connection is interrupted, the next request returns sanitized 503 with zero simulator
calls, then the same instance succeeds after the proxy resumes. CLI issuance, hashing, and storage
checks stay in the provisioning/admin tests rather than being duplicated here.

## Deferred live-provider tests (unrun)

`executor_integration_test.rs` still contains live OpenAI cases. They are **`#[ignore]`** and also
require `OPMUX_LIVE_PROVIDER_TESTS=1`. Inherited `OPENAI_API_KEY` does **not** activate them. They
are unrun in routine CI and local test commands.

Do not run them unless live verification is explicitly requested. They can incur charges.

```bash
OPMUX_LIVE_PROVIDER_TESTS=1 cargo test -p gateway --test executor_integration_test -- --ignored --nocapture
```

## Container image check

`scripts/check-container.sh` builds `opmux-gateway:mvp` from the locked Dockerfile, runs it as UID
`65532`, publishes `127.0.0.1:38086` only, and exercises persisted authentication against the owned
local Supabase plus an owned loopback simulator. Invalid keys return `401` with zero generation
calls. A second run against an untrusted local TLS simulator must return sanitized `502` without
using `curl -k`. Building the image does not need production database or provider credentials.

```bash
bash scripts/check-container.sh
```

## Additional Task 13 artifacts

- End-to-end integration coverage for resilience behavior in `observability_integration_test.rs`
  (circuit-open transition under repeated upstream failures).
- Performance/load testing guide in `PERFORMANCE_LOAD_TESTING.md`.
- Load test runner script at `scripts/run-load-tests.sh`.
